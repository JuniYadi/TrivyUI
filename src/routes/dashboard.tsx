export function DashboardPage() {
  return (
    <main style={{ fontFamily: "system-ui", lineHeight: 1.6, padding: "2rem" }}>
      <h1>TrivyUI Dashboard</h1>
      <p>Phase 1 bootstrap is running.</p>
      <p>
        Fullstack Bun app ✅ — SPA shell ✅ — SQLite baseline ✅
      </p>
    </main>
  );
}
